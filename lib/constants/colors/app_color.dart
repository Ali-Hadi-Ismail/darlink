import 'package:flutter/material.dart';

Color backGroundColor = Color(0xFF37375F);
Color cardBackGroundColor = Color(0xFF3C3A4F);
Color cardPersonColor = Color(0xFF663434);
Color textColor = Colors.white;

class AppColors {
  static const Color primaryRed = Color(0xFFD32F2F); // Red for buttons, app bar
  static const Color black = Color(0xFF000000); // Black for text and buttons
  static const Color white = Color(0xFFFFFFFF); // White for background
  static const Color darkGray =
      Color(0xFF4F4F4F); // Dark gray for secondary text
  static const Color lightGray = Color(0xFFBDBDBD); // Light gray for dividers
}
